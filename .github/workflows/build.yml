name: default-pipeline
on:
  push:
    tags:
      - v*
jobs:
  pre-build-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "^1.24.2"
      - name: Install gofumpt
        run: go install mvdan.cc/gofumpt@e350b3cdcd03387e0e77f9fd637c27a6d30e1a3f
      - name: Check for compilation errors
        run: go build ./cmd/dhee
      - name: Check formatting
        run: gofumpt -w -l . && git diff --exit-code
      - name: Run tests
        run: go test ./...
  build-docker-image:
    runs-on: ubuntu-latest
    needs: [pre-build-checks]
    env:
      TAG_NAME: ${{ github.ref_name }}
      IMAGE_NAME: ghcr.io/mahesh-hegde/dhee
      IMAGE_NAME_LAMBDA: ghcr.io/mahesh-hegde/dhee-lambda
    steps:
      - uses: actions/checkout@v4
      - name: Docker login
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u $ --password-stdin
      
      # Build and push server image
      - name: Docker build (server)
        run: docker build -t "${IMAGE_NAME}:latest" --platform linux/amd64 -f Dockerfile .
      - name: Push server latest image
        run: docker push $IMAGE_NAME:latest
      - name: Tag server image
        run: docker tag $IMAGE_NAME:latest $IMAGE_NAME:${TAG_NAME#v}
      - name: Push server tagged image
        run: docker push $IMAGE_NAME:${TAG_NAME#v}
      
      # Build and push Lambda image
      - name: Docker build (lambda)
        run: docker build -t "${IMAGE_NAME_LAMBDA}:latest" --platform linux/amd64 -f Dockerfile.lambda .
      - name: Push lambda latest image
        run: docker push $IMAGE_NAME_LAMBDA:latest
      - name: Tag lambda image
        run: docker tag $IMAGE_NAME_LAMBDA:latest $IMAGE_NAME_LAMBDA:${TAG_NAME#v}
      - name: Push lambda tagged image
        run: docker push $IMAGE_NAME_LAMBDA:${TAG_NAME#v}
